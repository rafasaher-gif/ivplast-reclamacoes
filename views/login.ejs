<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>IVPLAST | Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body{background:#f3f5f7;}
    .card-login{max-width:460px;margin:60px auto;border:0;border-radius:14px;box-shadow:0 10px 30px rgba(0,0,0,.08);}
    .brand{display:flex;align-items:center;gap:12px;justify-content:center;margin-bottom:10px;}
    .brand img{height:44px;}
    .muted{color:#6c757d;}
    .btn-primary{background:#1177cc;border-color:#1177cc;}
    .btn-primary:hover{background:#0f66ad;border-color:#0f66ad;}
    .footer{font-size:12px;color:#98a2b3;text-align:center;margin-top:18px;}
  </style>
</head>
<body>
  <div class="card card-login">
    <div class="card-body p-4 p-md-5">
      <div class="brand">
        <img src="https://dummyimage.com/140x44/ffffff/1177cc.png&text=IVPLAST" alt="IVPLAST"/>
      </div>
      <h5 class="text-center mb-0">SOLUÇÕES DE BUCHAS</h5>
      <p class="text-center muted mb-4">IVPLAST</p>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger py-2"><%= error %></div>
      <% } %>

      <form method="POST" action="/login" autocomplete="on">
        <div class="mb-3">
          <label class="form-label">Email</label>
          <input name="email" type="email" class="form-control" placeholder="Email" required />
        </div>

        <div class="mb-2">
          <label class="form-label">Senha</label>
          <div class="input-group">
            <input id="senha" name="senha" type="password" class="form-control" placeholder="Senha" required />
            <button type="button" class="btn btn-outline-secondary" onclick="toggleSenha()" title="Mostrar/ocultar">
              👁
            </button>
          </div>
        </div>

        <div class="mb-3">
          <a href="/esqueci-senha" class="small text-decoration-none">Esqueci minha senha</a>
        </div>

        <button class="btn btn-primary w-100 py-2" type="submit">Entrar</button>

        <div class="text-center mt-3">
          <span class="muted small">Ainda não tem conta?</span>
          <a class="small text-decoration-none" href="/register">Registrar</a>
        </div>

        <div class="footer mt-4">IVPLAST Soluções de Buchas © <%= new Date().getFullYear() %></div>
      </form>
    </div>
  </div>

  <script>
    function toggleSenha(){
      const el=document.getElementById('senha');
      el.type = (el.type === 'password') ? 'text' : 'password';
    }
  </script>
</body>
</html>
